name: Telegram AI Quiz Scheduler

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Call send.php
        run: |
          curl -L "https://brni.great-site.net/send.php"
